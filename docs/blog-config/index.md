---
sidebar_position: 0
---

# 博客配置

本区集中放**本站点**与 Docusaurus **博客 / 文档结构 / 顶栏侧栏**相关的说明，避免和「想法与项目」等内容混在一起。

## 本区目录

| 文档 | 说明 |
|------|------|
| [`_category_.yml` 模板（完整注释样例）](/docs/blog-config/category-yml-template) | 与根目录 `config-examples/_category_.yml` 同步，可直接复制到 `docs/.../` 使用 |
| [分类元数据 `_category_.yml`](/docs/blog-config/docusaurus-category-json) | 侧栏分类各字段含义（本站 `docs/` 已统一使用 YAML；页内亦说明 JSON 等价写法） |
| [预设与博客开关](/docs/blog-config/preset-and-blog) | `preset-classic` 中关闭内置 `blog`、本地搜索 `indexBlog` 等 |
