# 待决与备忘

随手记**还没想清楚**或**要做决策**的点。已有结论的迁到 [架构与数据流](../architecture/index.md) / [通信与协议](../protocol/index.md)。

## 已决（便于扫一眼）

- [x] **传输层**：**WebSocket（WSS）**；扩展主动连后端，长连接上双向传观测、指令与回执（见架构 / 协议）。

## 仍待跟踪

- [ ] 单次下发：**单步** vs **多步调用计划**；失败重试与幂等。  
- [ ] 「类 API」是否未来演进为扩展侧真实 HTTP（本地端口）——若做，安全模型单独立项。  
- [ ] 多 Tab、多账号与上下文的绑定方式。  
- [ ] 是否区分业务 WebSocket 与调试通道（或同连接多 topic）。

## 可继续追加

- 在此用 checklist 或短列表即可；条目变长时再拆成独立 `.md`。
